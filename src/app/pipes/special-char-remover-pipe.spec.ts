import { SpecialCharRemoverPipe } from './special-char-remover-pipe';

describe('SpecialCharRemoverPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialCharRemoverPipe();
    expect(pipe).toBeTruthy();
  });
});
